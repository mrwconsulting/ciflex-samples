version: '0.2'
reports:
  junit:
    files:
      - 'junit.xml'
    base-directory: 'python-poetry/build/reports'
    file-format: 'JUNITXML'
    discard-paths: no
  coverage:
    files:
      - 'coverage.xml'
    base-directory: 'python-poetry/build/reports'
    file-format: 'COBERTURAXML'
    discard-paths: no