version: '0.2'
reports:
  image-reports:
    files:
      - 'jib-report.xml'
      - 'buildpack-report.xml'
    base-directory: 'springboot-gradle/build'
    file-format: 'JUNIT'
    discard-paths: no
